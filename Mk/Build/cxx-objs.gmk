ifndef _${BP}_rules_cxx-objs
_${BP}_rules_cxx-objs=

ccvar:=CXX
langname:=C++
flagpref:=CXX

# Needs to be fixed:
# Make sure user isn't doing fancy tricks by passing flags to echo
SRCS:=$(shell echo ${${BP}.srcs.cxx})

include ${MK}/Build/common/ccc-objs.gmk

endif
